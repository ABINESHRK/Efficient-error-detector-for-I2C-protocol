# Efficient-error-detector-for-I2C-protocol
The crc_generator module is designed to generate a Cyclic Redundancy Check (CRC) for a 16-bit input data word using a 4-bit polynomial. CRC is commonly used in digital communication systems to detect transmission errors.
